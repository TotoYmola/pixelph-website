import {readSession,isAdmin,json} from '../../../_lib/auth.js';
export async function onRequestPost({request,env,params}){const user=await readSession(request,env);if(!isAdmin(env,user))return json({error:'Staff access required.'},403);if(!env.DB)return json({error:'Database not configured'},503);const b=await request.json();if(!['approved','rejected'].includes(b.status))return json({error:'Invalid status'},400);if(b.status==='rejected'&&!String(b.reason||'').trim())return json({error:'Rejection reason required'},400);const now=new Date().toISOString();const result=await env.DB.prepare('UPDATE applications SET status=?,review_reason=?,reviewed_by=?,reviewed_at=?,updated_at=? WHERE id=?').bind(b.status,String(b.reason||'').trim(),user.id,now,now,params.id).run();if(!result.success)return json({error:'Update failed'},500);return json({ok:true})}
